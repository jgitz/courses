// if phone number has 10 digits return true else false

//to find the length of a string we use <variable>.length method

// length method only works with strings. So, phone number should be given as string

var phone = (num) => num.length == 10 ? true : false; // num.length is the length method

console.log(phone('12345678'));