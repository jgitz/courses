//to check if num is negative or positive

var num = -15;

if (num < 0) {
    console.log(`the number is ${num} which is negative.`)
}

else {
    console.log(`the number is ${num} which is positive`)
};

//shorthand of if.. else. "?" denotes if and ":" denotes else

console.log("************the below is the output of if.. else short hand i.e, console.log(num<0?'negative':'poaitive')*********")

console.log(num<0?"negative":"positive");