// to convert an object to string use method... JSON.stringify(<object_variable_name>)

// to convert an string back to object use method... JSON.parse(<string_variable name> || "") ....**** || "" is important as in syntax of parse, it expects only a string or a null .... so "" specifies null 