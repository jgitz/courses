/* &&&&& ROUTERLINK ATTRIBUTE

usually in html inside an ANCHOR tag we give the URL IN HREF="PATH" attribute.... but in ANGULAR, there is no HREF

....in ANGULAR we use attribute calle routerLink="<PATH>" to link the anchor tag to a URL instead of HREF

eg <a routerLink = "<path>">REGISTER</a>

*** routerLink... L is capital letter


*/