// write an arrow function to add two numbers

const add = (first_number, second_number) => {
    let result = first_number + second_number
    console.log(`the sum is ${result}`)
}

add(1,2) // o/p: the sum is 3