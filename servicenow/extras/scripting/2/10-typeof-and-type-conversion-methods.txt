- typeof(<>) : use this to know the type of a variable 

- <>.toString() : converts the variable <> to a string 

- parseInt(<>): converts the variable <> to a number

- parseFloat(<>) : converts the variable <> to a floating point number 

  ***** you can only see the DECIMAL POINT only when a mathematical operation is done