/* 
- INDEX OF METHOD: only returns the index number if the element is PRESENT inside the ARRAY

  * findIndex returns index number if the element satisfies a certain CONDITIONS

- BOTH indexOf and findIndex method returns -1 if no element is found or satisfies the condition.
*/