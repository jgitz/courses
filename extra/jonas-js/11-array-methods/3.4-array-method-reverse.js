/* 

REVERSE METHOD: is a method which reverses element position of an array

** MUTATES the original array

*/

const array = [1, 2, 3, 4, 5]

console.log(array.reverse()) // [ 5, 4, 3, 2, 1 ]
console.log(array) // [ 5, 4, 3, 2, 1 ] ; ** mutates the original array