/* 

- set interval function : takes the following

      1) callback function
      2) time in milliseconds
      3) arguments for the callback function

    ** set interval runs the callback function CONTINOUSLY after at each interval after the given time in milliseconds

- clear Interval function: is used to stop the running set Interval callback function (which is stored in a variable)

*/