/* 
logical operators are  AND, OR, and NOT (!)

AND operator( && ) : only works when both conditions are true

OR operator ( || ) : works when ONE of the conditon is true

NOT operator ( ! ) : use it before anything to obtain the reverse boolean value

*/

//eg

const color1 = "green"
const color2 = "red"

if ( color1 == "green" && color2 == "green") {
    console.log("pass")
} else {
    "make both colors same"
}



    