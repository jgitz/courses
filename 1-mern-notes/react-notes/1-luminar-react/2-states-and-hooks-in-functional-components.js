/* 
q) What is a STATE? : state is an object inside a component that holds information.


....inside a component... the data inside the return() is STATIC.... we need STATES to use DYNAMIC data....

..... YEARS BACK.... class based component was only way to create states... so CLASS based components were called... STATEFUL component... and FUNCTIONAL components were called STEATELESS components....

....but NOW.... using HOOKS we can make use of STATES inside FUNCTIONAL COMPONENTS



*/

/* +++++HOOKS+++++++++++++++++++++

inside a functional component, we mae STATES using HOOKS.....

..... there are many TYPES of hooks inside REACT

1) useState hook: is used to create a hook inside functional component

*/